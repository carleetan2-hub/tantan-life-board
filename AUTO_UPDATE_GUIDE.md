# Tantan Life Board 自动更新指南

## 一、为什么你之前看不到更新？

GitHub Pages 有三层缓存机制导致更新不会立即生效：

1. **GitHub CDN 缓存**（5~10 分钟）
   - GitHub 使用全球 CDN 加速，新文件需要一定时间同步到各地节点

2. **Service Worker 离线缓存**（PWA 功能）
   - 为了让网页离线可用，浏览器会把页面文件缓存到本地
   - 之前的缓存策略比较激进，导致总是返回旧版本

3. **浏览器本地缓存**
   - 浏览器会自动缓存 CSS、JS、图片等静态文件

**现在我已经彻底解决了这个问题**，从三个层面确保你能第一时间看到更新：

---

## 二、自动更新方案（已内置）

### 方案 1：页面自动检测新版本（推荐）

打开工作台后，页面会在 **5 秒后自动检查**是否有新版本：

- 如果发现新版本，顶部会弹出紫色提示条：「🎉 发现新版本，建议刷新页面获取最新功能」
- 点击「立即刷新」即可一键更新
- 点击「✕」可忽略提示（1 小时内不再弹出）

**技术原理**：页面会悄悄请求 `index.html`，对比其中的版本号。如果远程版本号大于本地版本号，就提示更新。

### 方案 2：Service Worker 智能缓存

新的 Service Worker 采用更聪明的策略：

- **主文件（HTML）**：始终从网络获取最新版本，网络失败时才用缓存
- **其他资源**：先返回缓存（保证秒开），后台悄悄更新缓存
- **版本控制**：每次更新都会更换缓存名称，旧缓存自动清理

### 方案 3：版本号强制刷新

每个版本的链接都带有版本号参数，例如：

```
https://你的用户名.github.io/仓库名/tantan-workbench.html?v=25
```

版本号变化时，浏览器会认为是新文件，自动绕过缓存。

---

## 三、如何更新你的 GitHub Pages？

### 方法 A：一键部署脚本（最简单）

我为你准备了一个部署脚本，只需一步即可推送更新：

#### 首次使用（配置一次）

1. 把 `deploy.sh` 放入你的仓库根目录
2. 打开终端，进入仓库目录，执行：
   ```bash
   chmod +x deploy.sh
   ```
3. 确保你的仓库已关联 GitHub：
   ```bash
   git remote -v
   ```
   如果显示空白，先执行：
   ```bash
   git remote add origin https://github.com/你的用户名/你的仓库名.git
   ```

#### 每次更新（只需一行命令）

```bash
./deploy.sh "这里写更新说明"
```

例如：
```bash
./deploy.sh "修复睡眠统计bug，增加生词本功能"
```

脚本会自动完成：添加文件 → 提交 → 推送到 GitHub → 触发 Pages 重新部署

### 方法 B：GitHub Actions 全自动部署（进阶）

配置好后，**每次推送到 GitHub 都会自动部署**，无需手动操作。

#### 配置步骤

1. 把 `.github/workflows/deploy.yml` 放入你的仓库（保持目录结构）
2. 打开 GitHub 网页 → 进入你的仓库 → 点击 **Settings** → 左侧 **Pages**
3. **Source** 选择 **GitHub Actions**
4. 完成！以后每次 `git push` 都会自动部署

#### 如何确认 Actions 正常工作

- 推送到 GitHub 后，打开仓库页面 → 点击上方 **Actions** 标签
- 看到绿色的 ✅ 表示部署成功
- 看到红色的 ❌ 表示失败，点击可查看错误原因

### 方法 C：手动上传（备用）

如果你不想用命令行，仍然可以手动上传：

1. 下载我提供的最新代码压缩包
2. 解压后，把所有文件拖入你的 GitHub 仓库页面
3. 填写提交信息，点击 **Commit changes**
4. 等待 1~2 分钟，GitHub Pages 会自动重新构建

---

## 四、更新后立即看到效果的技巧

即使代码已经推送到 GitHub，浏览器可能还是显示旧版本。试试以下方法：

### 强制刷新（最快）

- **Windows**：按 `Ctrl + F5` 或 `Ctrl + Shift + R`
- **Mac**：按 `Cmd + Shift + R`
- **手机**：地址栏重新输入链接，或关闭标签页重新打开

### 清除 Service Worker 缓存

**Chrome/Edge**：
1. 按 `F12` 打开开发者工具
2. 切换到 **Application（应用）** 标签
3. 左侧找到 **Service Workers**
4. 点击 **Unregister（注销）**
5. 刷新页面

**Safari（iPhone/Mac）**：
1. 设置 → Safari → 清除历史记录与网站数据
2. 重新打开页面

### 使用隐身模式测试

打开浏览器的**无痕/隐身窗口**访问链接，这里没有任何缓存，可以看到最新版本。

---

## 五、文件版本对照表

更新时请确保以下文件的版本号一致：

| 文件 | 当前版本 | 作用 |
|------|---------|------|
| `index.html` | `?v=25` | 入口跳转页 |
| `tantan-workbench.html` | `APP_VERSION = 'v25'` | 主应用 |
| `tantan-sw.js` | `CACHE_NAME = 'tantan-workbench-v25'` | 缓存控制 |

**每次大更新后，这三个地方都需要同步更新版本号**（例如 25 → 26），确保所有缓存都被刷新。

---

## 六、常见问题

### Q1：提示条显示有新版本，但刷新后还是旧版？

可能 GitHub Pages 的 CDN 还没刷新。等待 2~3 分钟后再次刷新，或尝试在链接后加随机参数：

```
https://你的用户名.github.io/仓库名/tantan-workbench.html?v=999
```

### Q2：手机上看不到更新？

iOS Safari 的 PWA 缓存比较顽固：
1. 从主屏幕删除图标
2. 在 Safari 中打开最新链接
3. 重新「添加到主屏幕」

### Q3：如何确认当前运行的是哪个版本？

在浏览器控制台（F12 → Console）输入：

```javascript
APP_VERSION
```

会显示当前版本号，例如 `"v25"`。

### Q4：自动更新检测会影响页面速度吗？

不会。检测在页面加载 **5 秒后** 异步执行，且请求非常小（只有几 KB），不会阻塞任何功能。

---

## 七、总结

| 场景 | 推荐做法 |
|------|---------|
| 日常更新 | `./deploy.sh "更新说明"` |
| 不想用命令行 | 手动拖拽文件到 GitHub 网页 |
| 想完全自动化 | 配置 GitHub Actions |
| 看不到更新 | 强制刷新 `Ctrl+F5`，或清除缓存 |
| 手机上异常 | 删除主屏幕图标，重新添加 |

**核心要点**：
- 页面现在会自动检测新版本并提示你刷新
- Service Worker 采用智能策略，优先使用网络最新版本
- 一键部署脚本让更新变得非常简单
- 版本号机制确保缓存被彻底刷新
